#!/usr/bin/env ruby
#
# Collects complexity data for a git repo
#

require 'json'

OUTPUT_FILE = 'public/data/complexity.json'

dates=`git log --pretty="%ci"`
dates = dates.split("\n").map {|d|
  { date: d, complexity: rand(10) }
}

File.open(OUTPUT_FILE, 'w') { |f| f.write(dates.to_json) }
